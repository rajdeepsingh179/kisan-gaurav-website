{
  "name": "kisan-gaurav-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test": "node --test tests/*.test.js",
    "test:security": "node --test tests/*.test.js",
    "admin:create": "node scripts/create-first-admin.mjs"
  },
  "dependencies": {
    "@auth/core": "^0.41.3",
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
